apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meeseecs-box.fullname" . }}-test-connection"
  labels:
    {{- include "meeseecs-box.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meeseecs-box.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
