apiVersion: v1
kind: Service
metadata:
  name: {{ include "meeseecs-box.fullname" . }}
  labels:
    {{- include "meeseecs-box.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meeseecs-box.selectorLabels" . | nindent 4 }}
